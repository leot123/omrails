source 'https://rubygems.org'

gem 'rails', '3.2.12'
gem 'jquery-rails'
gem 'devise'
gem 'simple_form'

group :production do
	gem 'pg'
end

group :development, :test do
	gem 'sqlite3'
end

group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
  gem 'bootstrap-sass', '~> 2.3.0.1'
end

# After placing a gem, you must run bundle install in the terminal. What this does is go to the site and pull them all from the site. this is very important#
#it will tell you when its already installed, and wont do anything#
#if you every bundle after adding a gem, you have to restart your rails s in terminal#
